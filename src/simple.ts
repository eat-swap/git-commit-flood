// Since TypeScript is a superset of JavaScript, it can be used to write JavaScript code.
// And... The code below is copied from "simple.js"

let x = %d;

// this file cannot be executed directly.

for (let i = 0; i < x; i++) {
    console.log('%s');
}
